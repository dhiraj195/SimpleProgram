package com.trigent;

import java.util.List;
import java.util.stream.Collectors;

public class Customer {

	public static void main(String[] args) {
//		
//		List<Customer> uniqueCustomers= customerList.stream().collect(Collectors.toMap(Customer::getId, c -> (existing replacement) -> existing))
//			.values().stream().collect(Collectors.toList())	;
//		
//		Map<Integer , List<Customers>> gropuByIdCustomerId= uniqueCustomers.stream().collect(Collectors.groupingBy(Customer::getId));
	}
}
	